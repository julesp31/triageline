

lines from appt index, 59
        <%= link_to 'Edit', edit_appointment_path(appointment), class: 'btn btn-warning btn-sm text-white mr-2' %>
                <%= link_to 'Cancel', appointment_path(appointment), method: :delete, class: 'btn btn-danger btn-sm text-white', data: { turbo_confirm: 'Are you sure?' } %>


lines 52, 53

            <div>
            <%= button_to "Closed", toggle_status_appointment_path(appointment), method: :post, params: {status: "Closed"} %>
            </div>


<%= image_tag 'nurse_female.png', alt: 'nurse_icon', class: 'nurse-icons' %>


flex-direction: column;
    min-height: 100vh;
    align-self: flex-end;
    position: relative;
    
    transform: translateX(-50%); /* Center the button horizontally */
