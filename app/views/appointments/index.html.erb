<div class="container mt-5">
  <div class="text-center mb-4">
    <h1>Appointments</h1>
  </div>

  <div class="row">
    <% @appointments.each do |appointment| %>
      <div class="col-md-6 mb-4">
        <div class="card shadow-sm">
          <div class="card-body">
            <%= link_to appointment_path(appointment), class: 'text-decoration-none text-dark' do %>
              <h5 class="card-title lobster-regular"><%= appointment.appointment_date.strftime('%B %e, %Y') %> - <%= appointment.appointment_type %> - <%= appointment.status %></h5>
            <% end %>
            <p class="card-text roboto">Severity: <%= appointment.severity %></p>

            <div class="d-flex align-items-center mb-2">
              <%= image_tag 'doctor.png', style: 'width: 24px; height: 24px; margin-right: 8px;' %>
              <strong><%= appointment.appointment_date.strftime('%B %e, %Y') %></strong>
            </div>
            <div class="d-flex align-items-center mb-2">
              <%= image_tag 'doctor.png', style: 'width: 24px; height: 24px; margin-right: 8px;' %>
              <strong><%= appointment.appointment_date.strftime('%I:%M %p') %></strong>
            </div>
            <div class="d-flex align-items-center mb-2">
              <%= image_tag 'doctor.png', style: 'width: 24px; height: 24px; margin-right: 8px;' %>
              <strong><%= appointment.appointment_type %></strong>
            </div>
            <div class="d-flex align-items-center mb-2">
              <%= image_tag 'doctor.png', style: 'width: 24px; height: 24px; margin-right: 8px;' %>
              <strong><%= appointment.status %></strong>
            </div>

            <% if current_user.clinician? %>
              <div class="d-flex justify-content-end mt-3">
                <%= link_to 'Edit', edit_appointment_path(appointment), class: 'btn btn-warning btn-sm text-white mr-2' %>
                <%= link_to 'Cancel', appointment_path(appointment), method: :delete, class: 'btn btn-danger btn-sm text-white', data: { turbo_confirm: 'Are you sure?' } %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
